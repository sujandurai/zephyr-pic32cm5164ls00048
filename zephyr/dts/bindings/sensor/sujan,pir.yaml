description: PIR Motion Sensor

compatible: "sujan,pir"

include: sensor-device.yaml

properties:
  gpios:
    type: phandle-array
    required: true
    description: |
      GPIO pin connected to the PIR sensor output signal.
